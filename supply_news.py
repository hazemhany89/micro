"""\nsupply_news.py\n\nFetches supply data for a keyword from Google News (e.g., number of articles).\n"""\n\nfrom core.fetch_google_news import fetch_google_news\n\ndef fetch_news_supply(keyword, limit=20, start_date=None, end_date=None, source=None):\n    """\n    Fetches supply data for the given keyword from Google News.\n\n    Args:\n        keyword (str): The keyword to search for.\n        limit (int): Maximum number of articles to fetch.\n        start_date (str): Start date for filtering in format 'MM/DD/YYYY'.\n        end_date (str): End date for filtering in format 'MM/DD/YYYY'.\n        source (str): Filter articles by specific news source.\n\n    Returns:\n        dict: Dictionary with supply metrics and article data.\n    """\n    articles = fetch_google_news(keyword, limit, start_date, end_date, source)\n    \n    # Calculate basic metrics\n    article_count = len(articles)\n    sources = set([article['source'] for article in articles])\n    unique_sources_count = len(sources)\n    \n    return {\n        "keyword": keyword,\n        "article_count": article_count,\n        "unique_sources_count": unique_sources_count,\n        "articles": articles,\n        "sources": list(sources)\n    }\n